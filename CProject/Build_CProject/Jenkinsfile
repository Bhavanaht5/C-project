pipeline {
    agent any
environment{
    PATH = "/usr/bin:$PATH"
}
    stages {
        stage("build") {
            steps {
     cd /home/ec2-user/jenkins/workspace/CProject/CProject/Build_CProject      
     sh "make"
            }
        }
    }
}
